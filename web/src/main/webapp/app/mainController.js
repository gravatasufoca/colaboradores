/**
 * Created by bruno on 31/08/17.
 */
define(['msAppJs',

], function (app) {
    'use strict';

    /**
     * Controller da aplicação
     */
    console.info("mainController.js...........")
    app.controller('mainController', ['$scope',
        "$timeout",
        '$filter',
        '$state',
        '$rootScope',
        '$http',
        function ($scope,
                  $timeout,
                  $filter,
                  $state,
                  $rootScope,
                  $http) {




        }]);

    return app;
});